/* Intentionally empty for Classroom Explorations Hub release 2026.08.10.5+

Do not launch the Hub from Edublogs' separate JavaScript Enhancement field.
The proven production contract uses the direct immutable bootstrap <script>
inside the page-17 Custom HTML route shell.

The route-local bootstrap now owns release validation, asset loading, lifecycle
markers, duplicate protection, fallback preservation, and the recovered Hub
auto-scroll behavior.
*/
